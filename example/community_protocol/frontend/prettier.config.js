/** @type {import("prettier").Options} */
const config = {
    trailingComma: "all",
    tabWidth: 2,
    semi: true,
    singleQuote: true,
    jsxSingleQuote: true,
    printWidth: 100
};

module.exports = config;